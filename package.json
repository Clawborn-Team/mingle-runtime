{
  "name": "@clawborn/mingle-runtime",
  "version": "0.0.0",
  "description": "Local Mingle Runtime daemon — drives real provider integrations (Codex App Server, Claude Agent SDK, OpenClaw) behind a unified AgentRuntimeDriver contract.",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "4.1.10"
  },
  "optionalDependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.215",
    "zod": "^4.4.3"
  }
}
